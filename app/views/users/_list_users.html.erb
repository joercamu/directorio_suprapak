
<div class="row">
  <div class="col-md-8 col-md-offset-2 well top-space">
    <h1 class="title-ppal">Directorio Suprapak</h1>
    <div class="row">
      <div class="col-md-2">
        <%= link_to 'Nuevo contacto', new_user_path, class:"btn btn-primary" %>    
      </div>
      <div class="col-md-8">
        <%= form_tag(controller:"users",action:"search", method: "post") do %>
        <div class="input-group">
          <%= text_field_tag :value, nil, class:"form-control" %>
          <span class="input-group-btn">
            <%= submit_tag "Buscar", class:"btn btn-default" %>
          </span>
        </div>
        <% end %>
      </div>
    </div>
    <hr>
    
    <table class="table">
      <thead>
        <tr>
          <th>Avatar</th>
          <th>Nombre</th>
          <th>Cargo</th>
          <th>Email</th>
          <th>Ext</th>
          <th>Movil</th>
          <th>Departamento</th>
          <th></th>
        </tr>
      </thead>
    
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><a href="<%= user_path(user) %>"><%= image_tag user.cover.url(:small),class:"Brand" %></a></td>
            <td><%= user.name %></td>
            <td><%= user.office %></td>
            <td><%= user.email %></td>
            <td><%= user.ext %></td>
            <td><%= user.phone %></td>
            <td><%= user.departament.name %></td>
            <td><% link_to 'Show', user %>
            <% link_to 'Edit', edit_user_path(user) %>
            <%= link_to 'Eliminar', user, method: :delete, data: { confirm: 'Estas Seguro?' },class:"btn btn-danger" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
    
  </div>
</div>